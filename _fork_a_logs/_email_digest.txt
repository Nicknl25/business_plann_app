OPERATING_MODEL DIGEST -> EXECUTIVE MIRROR pushed (d340f3e). The executive no longer judges blind.

Built per your directive: a compact, token-aware digest of operating_model_json fed into the Mirror so
GPT sees the business portrait when it judges price/utilization/capacity proposals.
- mirror.build_operating_model_digest(ops_json) -> compact digest (business_type/consumer/stage,
  unit_name/description/cadence, unit_price, capacity, utilization_rate, capacity_driver,
  primary_growth_lever, sales_modality, geographic scope/coverage, description, competitive_advantage;
  long text capped). A digest, not the raw blob.
- build_mirror(ops_json=...) carries it in business_facts (Dict[str,Any] in MirrorContract -> no
  contract change). Runner now passes ops_json (it was loaded but never reached the executive).
- responder renders a "Business portrait (operating model)" block at judge-time.

PROOF (real Sunny operating_model_json -> real build_mirror -> real render_mirror_for_proposal, the
exact code path the live run uses): the executive's prompt for the V3 pricing tier now contains:
  Business portrait (operating model):
    business type: Donut Shop
    consumer type: consumer
    unit name: donut   |   unit price: 2
    units per week capacity: 1200   |   utilization rate: 0.75
    capacity driver: labor   |   sales modality: physical
    geographic scope: local  (Springfield, Sangamon County, IL)
    business description summary: Sunny Glaze Donuts is a single-location retail donut shop...
18 portrait fields surfaced. Tests green (78). So when GPT chooses to raise price/util/capacity it now
knows it's a $2 walk-in donut shop with labor-driven 1200/wk capacity -- not guessing.

This makes the executive's operating_model CHOICES sound (your Option-A direction: executive directs,
informed by the portrait). The remaining KEYSTONE is unchanged: margins must populate so the moves
APPLY -> real deltas. For the band-HAVING levers (drivers cogs%/marketing%/ga%/rd%, stage_ramp ceilings,
balance_sheet days) I can re-source _compute_lever_margins from the populated assemble_finmo_output_targets
(the same bands the realism gate enforces) instead of the empty cohort table. For operating_model
(no band -- still true), the executive picks direction from the portrait and the magnitude is the
solver's job (Option A). Continuing the keystone next.

Commits this session: adfecee, 0fd10a1, 71e9163, 5b72aa1, d340f3e (all pushed).
