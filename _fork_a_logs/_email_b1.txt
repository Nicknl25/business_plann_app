B1 WIRING PUSHED (5b72aa1) + the KEYSTONE blocker for real deltas precisely traced.

B1 (committed): revise_operating_model (applies unit_price/utilization_rate/units_per_week_capacity to
model_input revenue rows by driver; wired into session_factory + per-commit propagation) + revise_drivers
author-on-empty (V1 authors from the patch since drivers are deferred/empty at round-1). Smoke-tested,
live-exercised. This is the correct, necessary downstream wiring.

BUT re-running Sunny still no-progress-floors -- and the audit shows WHY, precisely:
every GPT-judged tier (V1-V7) logs original_value=None, proposed_value=None, applied_value=None.
The executive judges, but the PROPOSER is handing it EMPTY proposals (no numbers). So B1's revise tools
have nothing to apply.

KEYSTONE ROOT CAUSE (traced through 3 layers):
  restructure_proposer._type_a_proposal picks the tier's lever from result.lever_margins; when a lever
  has no margin it emits a PLACEHOLDER proposal with None values (lines 286-300).
  -> result.lever_margins is EMPTY for drivers + operating_model.
  -> because _compute_lever_margins (evaluate_plan.py:182-224) reads CURRENT lever values from
     plan_state, and the amalgamated plan_state (runner.py:1840-1850) has drivers={} (deferred) and NO
     operating_model key. Those current values live in the model_input ROWS, not plan_state.
  Net: no current values -> no margins -> no band target -> vacuous proposals -> no deltas -> floor.
This is a PRE-EXISTING issue that Wall A exposed (before, the META-halt meant the proposer was never
reached).

KEYSTONE FIX (next, well-specified): source current lever values from the LIVE model_input for the
cascade sections -- operating_model (price/util/capacity from revenue rows by driver; I already have the
inverse mapping in revise_operating_model) and drivers (COGS%/Marketing%/etc as %-of-revenue from the
live finmo) -- and feed them into _compute_lever_margins so lever_margins populates -> proposer computes
band targets -> B1 applies them -> REAL deltas. Caveat I will NOT skip: the model_input->lever_id mapping
must match the cohort_bands lever_id format exactly, and I must confirm cohort_bands_table.get_cohort_bands
actually returns drivers/operating_model bands (Luna's viability_standard noted cohort_bands_unresolved
even though the realism gate had 498 calibrated bands -- need to confirm which band source the margin
computation reads). Getting that mapping wrong = wrong margins = fake convergence, so it needs care +
a live verification cycle, not a tail-end rush.

STATE: Luna runs END-TO-END to a non_viable verdict (but cascade_landed_tier=0 -- same keystone: didn't
engage). Sunny reaches the pre-cash gpt-authorable gate and hard-fails on rent_percent_of_revenue ~217%
(expenses::Lease, not a cascade lever) -- a genuine deep non-viability + a B3-class hard-gate-crash to
reframe later. Four commits banked this session: adfecee (payroll), 0fd10a1 (Wall A), 71e9163 (Wall B /
end-to-end), 5b72aa1 (B1 wiring). All pushed. Handoff memory updated with the keystone.

NEXT: the keystone lever-margin sourcing fix, then re-run Sunny for real deltas, then all 3 drafts.
