REVENUE AUTHORING Task 1+4 pushed (3b4ea4d): the ONE enriched business compact now feeds the Mirror.

Extended the single operating_model digest (NOT a second digest) into GPT's full view of business reality
-- the grounding guardrail for revenue authoring + executive judging:
  - team (people_json): key people + planned year-1 roles/wages
  - target_market (target_market_json): consumer_type, positioning, segments, audience demo/income, B2B fields
  - market_demand (marketing_model_json -- added beyond the brief; it's the best grounding for revenue):
    reachable market, expected/required units, customers, capture rate, and the revenue that demand implies
    ("the sizing the top line must respect")

PROOF (real Sunny data -> real build_mirror -> real render): the executive prompt now shows the donut shop's
team (Jordan Lee owner, Maria Gonzalez baker), target market ($2 neighborhood, 18-45 / $35-75k), and market
demand (reachable 8000, expected 46,800 units/yr). Compact ~1054 tokens -- rich but not bloated. Tests green.

NEXT: Task 2 -- the revenue-authoring pass. GPT reads this compact and AUTHORS the revenue DRIVERS
(unit_price, volume/units, capacity, utilization, 20-quarter ramp); Python computes the revenue line. Runs
FIRST, before the cascade, so the plan is built on a GPT-authored, business-grounded top line. This resolves
the "revenue levers have no band" wall -- they're authoring inputs, not band-targeted levers. Then Task 3
(cascade re-invokes revenue authoring when revenue is the binding constraint, e.g. Sunny rent 217%).

Reframe noted + being implemented: non_viable is an adjustable FORECAST, not a verdict -- the system keeps
authoring/adjusting within what the compact says the business can really do, until the forecast is viable.

Reuse check (old operations_agent, backup branch): it's the free-authoring operator agent (memos/vetoes/
grid-review) -- wrong architecture for us (we do GPT-authors-drivers + Python-computes-line), but its operator
FRAMING is reusable for the authoring prompt ("what can this business realistically absorb across 20 quarters;
link growth to staffing/capacity/timing; reject ramps the operation can't support"). Extracting that voice.
