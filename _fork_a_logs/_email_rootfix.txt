ROOT DISEASE FIXED + proven on all three drafts. References GROUND, only VIABILITY gates. Universal, no
per-draft patches. Two commits: cd23c50 (FIX 2, stage), a4c01fd (FIX 1+3, gates flow).

The three drafts tripped three different rules -- all the same category error: cohort bands + stage/posture
rules + the verdict were wired as HARD GATES that 500'd on mismatch. All demoted to grounding so the run
RENDERS its forecast. Only genuine viability decides the verdict.

WHAT CHANGED (5 raise-sites, each was the next crash in the chain):
- Stage rules now match the REAL stage: operational-profitability requirements apply only to
  operational/mature; a startup/pre-revenue loss-tolerant ramp is VALID, not a crash. (FIX 2)
- Pre-cash gate: residual rent%/cost-ratio band conformance is advisory grounding, flows to finalize. (FIX 1)
- Finalize: "FINMO must match the stage_ramp contract path" is advisory -- revenue is GPT-authored (the root),
  so diverging from the contract's qoq path is information, not a defect. Structural integrity still gates.
- Acceptance verdict: a non-passing verdict RENDERS (200), it no longer 500s. non-viable is a forecast.
- Client-workbook export failure: surfaced in the 200 response, no longer 500s the run.

PROVEN (live, full stack):
- LUNA: completes (200) -> renders non_viable (EBITDA ~-19%, real economics).
- SUNNY: flows PAST the pre-cash gate + finalize -> completes (200). The rent wall is GONE: rent% is now
  REAL 21.9% -> 11.3% (under the ceiling by Q20), not the phantom 217%. Verdict non_viable on genuine
  viability (EBITDA -157% -> -70%: costs dwarf the modest $2-donut revenue) -- the legitimate gate, rendered
  not crashed.
- ANDERSON: clears round-1 stage_ramp (the original crash is gone; an earlier run reached finalize). This run
  then tripped a STRUCTURAL payroll-continuity check on its GARBAGE intake data (revenue=0 but COGS $978k,
  payroll $287k) + GPT-author variance -- the data-quality case you said to set aside, not the reference
  disease.

NEXT LAYER (separate, now UNBLOCKED): Luna + Sunny render non_viable because the CASCADE isn't engaging to
make them viable (cascade_landed_tier=0 -- the plans aren't being adapted toward profitability). The run no
longer crashes at/before the verdict, so the Fork A cascade-adaptation goal (move levers / re-author revenue
toward viability) can now run end-to-end. Also flagged: GPT payroll-author continuity variance; the
acceptance_score numeric-format workbook nit (now non-fatal).

Tests: 169 pass on touched suites (3 pre-existing fail_fast code-registry failures unrelated to this change).
