TASK: turn 18 - people-row canonicalization inside the ONE helper _merge_people_rows (intake_consult.py): add _canonicalize_person_row (name->full_name, title->role_title, annual_pay->annual_wage float, wage_source client_provided->client_override, years_experience->experience_years str, education_credentials->relevant_background; alias applied only when canonical key absent/empty, alias keys dropped), applied to every incoming row AND the standing rows before matching; rows with no identity after aliasing are DROPPED with a receipt (PEOPLE_PATCH dropped=[...] + a reply receipt surfaced like _derived_patch_receipt). Deal breakers: two-owner client dead-ended at INTAKE->POST_INTAKE (IntakeDraftContract 56 errors, no plan), and OEWS medians shown as the client's stated wages when the stated rows sit unmatched. No router change, no grouped-row change, no issue-571 probe, no legs, no re-bless.
BLAST-RADIUS: neighbor-check - the helper is shared high-fan-out code: five doors flow through it (people.people ~14499; people_finalize_done_adding ~18393; collection_extractor ~21620; people_finalize_review ~22046; people_finalize_ready ~22424) plus the recalc's owner-row pass that reads the stored rows. Touches: every people roster write; one step downstream: _apply_owner_pay_statement (finds owner by role_title -> now lands on the named owner instead of appending a bare Owner row), OWNER_ROW_UNIQUENESS fold, OEWS wage fill after the finalize merge, payroll_basis_people_roles row count, the boundary contract. Likely to change: the F3 first-owner landing (carried ruling), the roster reaching submit (4 rows not 12).
LOADING: replay_gate/HANDOFF.md turn-18 TASK; intake_consult.py _merge_people_rows / _person_row_identity / _label_person_row / PEOPLE_PATCH trace / _derived_patch_receipt surfacing / the five door call sites / _apply_owner_pay_statement; replay_gate/_payroll_directive_audit/r8_final_acceptance/mini_replay_raw_rows.py + backend_people_stage_slice.txt (log lines 103/107/117) + R8_FINAL_ACCEPTANCE.md (runner invocation, seed_two_owner.txt); the item-5 pin file tests/test_*people*; scripts vs_r5_nomove.py + mini_census_owner_rows.py; python/contracts (PeopleJsonContract, IntakeDraftContract). Not loading: router, engine, workbook builder, legs.
VERIFY: neighbor-check - pins (one new tests file: verbatim TURN_INTENT rows replayed in order -> 4 rows, model_validate passes, 118,000/104,000 client_override; identity-less row dropped with receipt; legacy name-beside-full_name keeps full_name; OEWS pass leaves client_override untouched; the four model-output doors' existing pins + one raw-row case each through _merge_model_roster; red-proof at HEAD 79b7fb4 for the right reason 12 rows / 56 errors); named neighbors: recalc no-move on the nine directive drafts (vs_r5_nomove.py --label r9, byte-unmoved), F5-class Sumac nameless-Owner drafts (55 + Halbrook 6bddab1a) keep the bare Owner row (identity ('title','owner')), hold census 102/0; VERIFY FORWARD hard case: LIVE dual-agent intake on :5050 (restart, ONE listener, pid quoted) with seed_two_owner.txt through SUBMIT and BUILD, freeze left FROZEN - PEOPLE_PATCH lines, roster count, owner wages, current_payroll, owner_compensation 18,500, Checks!B2 quoted; canary skip (the live intake is the forward proof); legs: gate --only R31,R32 before/after (2). Every job foreground.
