"""THE DOCUMENT SHELL (2026-08-30).

Nothing here writes prose. theme.py owns every chart's look (one visual
identity, the workbook's dataviz-validated palette); renderer.py builds the
Word object that proves rules 21/22/23 with placeholder text; probe.py reads
the produced .docx back and feeds the craft checks - a document that cannot
be probed FAILS, it does not pass by default.
"""
