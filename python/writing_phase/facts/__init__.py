"""THE FACT CATALOGUE (2026-08-30).

Rule 17 means GPT never computes, so anything worth saying with a number in it
has to exist here before generation starts. Built BACKWARD from sentences.py -
the observations a consultant would make - not forward from the tables, because
a catalogue shaped by what is convenient to query ends up thin, and a thin
brief fills itself with framing that passes every check while saying nothing.

catalog.py   the Fact, the ABSENT sentinel, provenance, the formatters
sentences.py the observations and the fact keys each one needs
build.py     the computations, one builder per namespace, against real drafts
"""
