Assume you are allowed to use every listed variable as part of one coherent repair plan for this actual company.
Realistically, what would make this business profitable as soon as it can become profitable without breaking business reality?
Use the full company context and behave like an operator trying to make the company truly work, not just cosmetically improve.
Business timing matters. When the context includes milestones with `timing` and `timing_months_max`, and people context includes `inferred_roles` with `months_until_hire` and `annual_wage`, factor that timing into the quarter path in a meaningful real-world way.
Do not treat staffing timing, milestone timing, or compensation as fixed if they are economically incoherent. You may move timing earlier or later, and you may raise, lower, defer, or phase compensation if that is what a real operator would do.
If you change timing or compensation, make the rest of the plan stay coherent: capacity, utilization, demand build, payroll load, overhead, and profitability timing should move in a believable way with it.
For rows labeled with units like `(% of Revenue)` or `(% of LTD)`, use decimal ratios instead of dollar amounts.
